services:
  - type: web
    name: darkself-ai-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn render_proxy:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
